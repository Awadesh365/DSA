
// nPr where n is number of items and r is how many of them are selected,
// in programing generally all of them are selected so generally r = n.
#include <bits/stdc++.h>
using namespace std;
// #define possibilities(x) while (next_permutation(x.begin(), x.end()))

int main()
{
    // number of permutations of this array
    int n = 3;
    int Arr[n];

    vector<int> a = {23, 32, 434, 54, 46, 65, 675};
    cout << next_permutation(a.begin(), a.end());

    // factorial of n
}